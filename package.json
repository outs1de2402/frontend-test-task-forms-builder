{
  "name": "google-forms-lite",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "lint": "npm run lint -w client",
    "generate": "npm run generate -w client"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
